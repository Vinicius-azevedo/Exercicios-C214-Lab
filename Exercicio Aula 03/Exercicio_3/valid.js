const calculadoraLimit = {
    val1: {
        type: 'number',
    },

    val2: {
        type: 'number',
    }
};

const limite = {
    lado: {
        type: 'number',
    }
};

module.exports = { calculadoraLimit, limite };